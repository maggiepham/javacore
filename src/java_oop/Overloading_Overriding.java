package java_oop;

public class Overloading_Overriding {
	
	/*
	 * Overloading
	 * is used to increase the readability of the program
	 * is performed within class
	 * parameter must be different
	 * is the example of compile time polymorphism
	 * cannot be performed by changing return type of the method only. Return type can be same or different in method overloading
	 */
	
	/*
	 * Overriding
	 * is used to provide the specific implementation of the method that is already provided by its super class
	 * occurs in 2 classes that have inheritance relationship
	 * parameter must be same
	 * is the example of run time polymorphism
	 * Return type must be same or covariant in method overriding
	 */

	public static void main(String[] args) {
		
	}

}
